<div>
    <%= question.title %>
    <%= link_to 'Show', question_path(question) %>
    <%= link_to 'Edit', question_path(question) %>
    <%= link_to 'Delete', question_path(question),
                method: :delete,
                data: {confirm: "Are you sure?"}
    %>
</div>